{ 
  "query": " SELECT COUNT(\"alert.severity\"), \"alert.severity\" FROM \"suricata-*\" WHERE \"alert.severity\" IS NOT NULL AND \"@timestamp\" > CURRENT_TIMESTAMP - INTERVAL 5 MINUTES GROUP BY 2"
}
